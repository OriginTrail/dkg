# DKG V10 Agent Context

DKG V10 is a decentralized knowledge network and protocol for verifiable agent memory. A DKG node is the local gateway into that network.

Core model:

- DKG Node: daemon, API, auth, wallets, storage, networking, UI, and integrations.
- Context Graph: scoped knowledge domain, also shown as project in the Node UI.
- Working Memory: private drafts.
- Shared Working Memory: peer-visible shared knowledge.
- Verified Memory: on-chain finalized knowledge.
- Knowledge Asset: published graph data with provenance and integrity; one ERC-721 per publish, holding one or more entities, addressed by a stable UAL.
- Publishing Conviction Account: V10 publisher commitment account with current `dkg pca ...` and `/api/pca/*` surfaces.
- Staker conviction: contract-backed NFT position model; do not assume a public operator how-to unless the exact workflow is documented.
- x402 knowledge commerce and context oracles: roadmap concepts, not current command references.

Current docs:

- Start at docs/README.md.
- First-time install and agent setup lives in docs/getting-started/quickstart.md.
- Workflow routes live under docs/use-dkg/.
- System model and architecture routes live under docs/how-dkg-works/.
- Reference routes live under docs/references/.
- Agent context projections live under docs/agent-context/.
- Official copied program/legal pages live under docs/active-now/ (DKG V10 bounty programs) and docs/getting-started/dkg-v10-t-c.md.

Operational contract:

- Use packages/cli/skills/dkg-node/SKILL.md for exact tool names, HTTP routes, and payload rules.
- Prefer runtime tools over raw HTTP when the runtime exposes them.
- Use memory search for free-text recall.
- Use SPARQL query for exact graph patterns.
- Use named graph patterns when querying sub-graph-routed data.
- Write first to Working Memory.
- Share to Shared Working Memory before peers can rely on it.
- Publish to Verified Memory only with explicit finality intent and available funds.

Do not use archived docs as current context.
