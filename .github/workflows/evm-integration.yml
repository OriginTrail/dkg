name: EVM Integration Tests

on:
  push:
    branches: [main, testnet-canary]
  pull_request:
    branches: [main, testnet-canary]
    types: [opened, synchronize, reopened, labeled, unlabeled]
  merge_group:
  workflow_dispatch:

concurrency:
  group: evm-integration-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

# Minimum-needed token scope (see ci.yml header for rationale).
permissions:
  contents: read

jobs:
  plan:
    name: Plan EVM delta
    runs-on: ubuntu-latest
    timeout-minutes: 5
    outputs:
      evm_matrix: ${{ steps.plan.outputs.evm_matrix }}
      plan_json: ${{ steps.plan.outputs.plan_json }}
    steps:
      # The merge candidate is untrusted input: use it only to calculate the
      # changed-file set, and never execute its CI planner or validator here.
      - name: Checkout merge candidate
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
          fetch-depth: 2
          path: candidate
      # Keep CI policy outside the merge candidate and pin it to an immutable,
      # reviewed commit. Policy updates deliberately rotate this SHA.
      - name: Checkout trusted CI controller
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: OriginTrail/dkg
          ref: 780f14aa60c39bdca788967121085c3c0d82d85c
          persist-credentials: false
          path: trusted-ci
          sparse-checkout: |
            scripts/ci/plan-ci.mjs
            scripts/ci/assert-ci-results.mjs
            scripts/lib/ci-delta.mjs
            scripts/lib/ci-results.mjs
          sparse-checkout-cone-mode: false
      - name: Compute fail-closed EVM plan
        id: plan
        env:
          EVENT_NAME: ${{ github.event_name }}
          MERGE_SHA: ${{ github.sha }}
          PR_LABELS: ${{ toJSON(github.event.pull_request.labels.*.name) }}
          SAMPLE_KEY: ${{ github.event.pull_request.head.sha || github.sha }}
          DELTA_ENABLED: ${{ vars.CI_DELTA_ENABLED == 'true' && (github.base_ref == 'main' || github.base_ref == 'testnet-canary') }}
        run: |
          set -euo pipefail
          CHANGES_FILE="${RUNNER_TEMP}/ci-name-status.z"
          : > "${CHANGES_FILE}"

          if [[ "${EVENT_NAME}" == "pull_request" ]]; then
            if [[ -z "${MERGE_SHA}" ]]; then
              echo "::error::Missing pull-request merge SHA"
              exit 1
            fi
            git -C candidate cat-file -e "${MERGE_SHA}^{commit}"
            # First parent of the synthetic merge = the CURRENT base tip; the
            # event's pull_request.base.sha is a stale snapshot that misroutes
            # PRs to full CI after unrelated merges to base (see ci.yml).
            BASE_SHA="$(git -C candidate rev-parse "${MERGE_SHA}^1")"
            git -C candidate diff --name-status -z \
              "${BASE_SHA}" "${MERGE_SHA}" > "${CHANGES_FILE}"
          fi

          PLAN_EVENT="${EVENT_NAME}"
          if [[ "${EVENT_NAME}" == "pull_request" && "${DELTA_ENABLED}" != "true" ]]; then
            PLAN_EVENT="pull_request_delta_disabled"
          fi

          node trusted-ci/scripts/ci/plan-ci.mjs \
            --event "${PLAN_EVENT}" \
            --changes-z "${CHANGES_FILE}" \
            --labels-json "${PR_LABELS:-[]}" \
            --sample-key "${SAMPLE_KEY}" \
            --github-output "${GITHUB_OUTPUT}" \
            --summary "${GITHUB_STEP_SUMMARY}"

  # Build the union of selected package dependency closures once. Consumers
  # restore compiler outputs from this run, then deploy into their own nodes.
  build:
    name: Build EVM integration packages
    needs: plan
    if: needs.plan.outputs.evm_matrix != '[]'
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Cache matching Hardhat compiler outputs
        uses: actions/cache@55cc8345863c7cc4c66a329aec7e433d2d1c52a9 # v6.1.0
        with:
          path: |
            packages/evm-module/artifacts
            packages/evm-module/cache
          # Keep compiler flavors and dependency/config revisions separate.
          # Hardhat still validates compilation after restoration.
          key: evm-integration-hardhat-${{ runner.os }}-${{ runner.arch }}-${{ hashFiles('pnpm-lock.yaml', 'packages/evm-module/package.json', 'packages/evm-module/contracts/**/*.sol', 'packages/evm-module/hardhat*.ts', 'patches/**') }}

      - name: Build selected EVM dependency closures
        env:
          EVM_SCOPES_JSON: ${{ needs.plan.outputs.evm_matrix }}
        run: node scripts/ci/build-evm-integration.mjs

      - name: Package integration build outputs
        run: |
          set -euo pipefail
          mapfile -t OUTPUTS < <(find packages -maxdepth 2 -type d -name dist | sort)
          test "${#OUTPUTS[@]}" -gt 0
          test -d packages/evm-module/artifacts
          test -d packages/evm-module/cache
          tar -czf /tmp/evm-integration-build.tgz "${OUTPUTS[@]}" \
            packages/evm-module/artifacts packages/evm-module/cache

      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: evm-integration-build
          path: /tmp/evm-integration-build.tgz
          retention-days: 1
          if-no-files-found: error

  # All three matrix scopes (chain, publisher, agent) are TORNADO-tier per
  # `dkgv10-spec/CRITICALITY_CATEGORIZATION.md` §1 — this workflow drives
  # them against a real Hardhat node to catch EVM-side regressions the
  # unit-test lanes in `ci.yml` can't reach.
  evm-integration:
    name: "Tornado EVM integration: ${{ matrix.scope }}"
    needs: [plan, build]
    if: needs.plan.outputs.evm_matrix != '[]'
    runs-on: ubuntu-latest
    timeout-minutes: 30

    strategy:
      fail-fast: false
      matrix:
        scope: ${{ fromJSON(needs.plan.outputs.evm_matrix) }}

    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - uses: pnpm/action-setup@b906affcce14559ad1aafd4ab0e942779e9f58b1 # v4.3.0

      - uses: actions/setup-node@48b55a011bda9f5d6aeb4c2d9c7362e8dae4041e # v6.4.0
        with:
          node-version-file: .nvmrc
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - uses: actions/download-artifact@3e5f45b2cfb9172054b4087a40e8e0b5a5461e7c # v8.0.1
        with:
          name: evm-integration-build
          path: /tmp
      - name: Restore integration build outputs
        run: tar -xzf /tmp/evm-integration-build.tgz

      - name: Run selected EVM integration scope
        env:
          EVM_SCOPE: ${{ matrix.scope }}
        run: ./scripts/test-evm-integration.sh "${EVM_SCOPE}"

  evm-gate:
    name: EVM integration gate
    if: always()
    needs: [plan, build, evm-integration]
    runs-on: ubuntu-latest
    timeout-minutes: 3
    steps:
      - name: Checkout trusted CI controller
        uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          repository: OriginTrail/dkg
          ref: 780f14aa60c39bdca788967121085c3c0d82d85c
          persist-credentials: false
          path: trusted-ci
          sparse-checkout: |
            scripts/ci/plan-ci.mjs
            scripts/ci/assert-ci-results.mjs
            scripts/lib/ci-delta.mjs
            scripts/lib/ci-results.mjs
          sparse-checkout-cone-mode: false
      - name: Verify selected EVM scopes completed
        env:
          EVENT_NAME: ${{ github.event_name }}
          PLAN_JSON: ${{ needs.plan.outputs.plan_json }}
          NEEDS_JSON: ${{ toJSON(needs) }}
        run: node trusted-ci/scripts/ci/assert-ci-results.mjs --workflow evm
