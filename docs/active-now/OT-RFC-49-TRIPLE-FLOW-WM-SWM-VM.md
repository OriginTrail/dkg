# OT-RFC-49 — The Flow of Triples: WM → SWM → VM, and the Combined Public Facet

## 1. Purpose & audience

This document is for OriginTrail engineers working on the DKG V10 publish/sync stack. It explains the three memory layers a triple passes through — Working Memory (WM), Shared Working Memory (SWM), and Verified Memory (VM) — grounded in the actual code paths, and then specifies the OT-RFC-49 *combined public-facet* model: a private Context Graph (CG) carrying a small plaintext public projection that rides in the **same** VM publish and the **same** on-chain merkle root as its encrypted private payload. It is precise about what exists in code today versus what the combined model adds, and it names the single new capability required (cores serving the facet of a private CG openly).

## 2. The three memory layers

| Layer | What it is | Scope | Persistence | Who sees it |
|---|---|---|---|---|
| **WM** (Working Memory) | An agent's local, per-agent staging graph — the literal "assertion" graph — written append-only before any sharing. | Per-agent (`{addr}`) and per-KA, inside the owning node's own triple store. | Local named graph (`_working_memory/{addr}/{number}`, legacy `assertion/{addr}/{name}`) until promote/publish; sealed at finalize. | Only the owning node/agent. Nothing is gossiped or anchored. |
| **SWM** (Shared Working Memory) | The live collaboration layer: signed gossip envelopes carrying the WM triples, sender-key encrypted for private CGs, fanned out member-to-member. | All CG members; opaquely held by hosting cores. | Local `_shared_memory` graph on each member; raw ciphertext bytes in the file-backed `SwmHostModeStore` on cores (TTL + byte-capped). | Members (decrypt & apply); cores hold ciphertext they cannot read; the envelope's outer metadata leaks cleartext cgId + publisher. |
| **VM** (Verified Memory) | The finalized layer: a publisher batches content, computes a V10 merkle root, gathers core ACK co-signatures, and commits the root on chain. | Network-wide commitment; the canonical anchor for the KA. | On-chain KC record (`merkleRoot`, and for curated CGs `ciphertextChunksRoot`); plaintext public quads in `publicSnapshotStore`, ciphertext to cores. | Anyone can read the on-chain root; public quads are open; private payload is AEAD ciphertext gated to members. |

## 3. The journey of a triple

**Enter WM.** A caller hits `POST /api/knowledge-assets/:name/wm/write` (`packages/cli/src/daemon/routes/knowledge-assets.ts:793`) or the atomic create+write shortcut at line 593. This calls `agent.assertion.write` (`packages/agent/src/dkg-agent.ts:1702`), which normalizes the body: a `Quad[]` is used as-is; a JSON-LD document is split via `jsonLdToQuads` into `publicQuads` + `privateQuads` (both concatenated and written); a simple `{subject,predicate,object}[]` is mapped to quads. It then delegates to `publisher.assertionWrite` (`packages/publisher/src/dkg-publisher.ts:4364`).

**Graph resolution & guard.** `assertionWrite` resolves the WM named-graph URI via `wmGraphUri` (`dkg-publisher.ts:4223`): once a kaId is minted it returns `contextGraphLayerUri(cg, MemoryLayer.WorkingMemory, addr, number, sub)` = `did:dkg:context-graph:{cg}[/{sub}]/_working_memory/{addr}/{number}`, otherwise the legacy `contextGraphAssertionUri` = `.../assertion/{addr}/{name}` (`packages/core/src/constants.ts:277`, `:261`). Every incoming triple is re-stamped with that WM graph URI — `assertionWrite` maps only `{subject,predicate,object}` and sets `graph: graphUri`, so the input's own graph field is discarded (`dkg-publisher.ts:4373-4375`). Then `rejectUserAuthoredProtocolMetadata` (`dkg-publisher.ts:343`, called at `:4378` before the store insert) runs: it throws `ReservedNamespaceError` if any quad's **subject** matches a reserved prefix — `urn:dkg:file:` or `urn:dkg:extraction:` (`packages/publisher/src/reserved-subjects.ts:4`), case-insensitive (`findReservedSubjectPrefix` lowercases, `reserved-subjects.ts:10`) — and also rejects user-authored trust-level quads. The guard inspects `q.subject` only (`dkg-publisher.ts:333`), so these URNs remain legal as **objects** (e.g. `<entity> dkg:sourceFile <urn:dkg:file:...>`).

**Store.** `store.insert(quads)` (`dkg-publisher.ts:4379`) appends to the triple store (oxigraph by default) via the `TripleStore` interface (`packages/storage/src/triple-store.ts:49`; `:48` is the interface header, and the append is performed by the adapter, not this declaration). WM is append-only: there is no delete/replace. The draft graph itself was opened earlier by `assertionCreate` (`dkg-publisher.ts:4253`), which `createGraph(wmGraphUri)` (`:4341`), wrote the lifecycle record into `_meta` (`:4343-4352`), and stamped `<wmGraphUri> dkg:memoryLayer "WM"` (`:4354-4359`). The one legitimate producer of reserved-subject rows in WM is the import-file handler (`packages/cli/src/daemon/routes/knowledge-assets-import.ts`), which writes `urn:dkg:file:{keccak256}` descriptor blocks and `urn:dkg:extraction:{uuid}` provenance (URN formats at `:1059-1060`) via a direct `agent.store.insert([...dataGraphQuads, ...metaQuads])` (`:1555`) that deliberately bypasses the guard (the bypass rationale is documented in the comment at `:1150`).

**Finalize (still WM, sealed before sharing).** `assertionFinalize` (`packages/agent/src/dkg-agent-publish.ts:1616`) reads the WM quads, filters `!isReservedSubject(q.subject) && !isTrustLevelQuad(q)` (line 1677) — the exact filter promote uses (`dkg-publisher.ts:4605-4606`), so the seal hashes the post-strip set the share/publish path will actually carry. It `skolemizeByEntity` (`:1714`), computes `computeFlatKCRoot` (aliased from `computeFlatKCRootV10`, `dkg-agent-publish.ts:107`) → `merkleRoot` (`:1733`), signs an EIP-712 `AuthorAttestation` (`buildAuthorAttestationTypedData` + `signTypedData`, `:2024`/`:2076`), and persists the seal as `_meta` triples keyed by the assertion-graph URI, plus pointer triples keyed on the lifecycle URI: `dkg:wmCurrentAssertion` (`WM_CURRENT_ASSERTION_PRED`, `:2124`), `dkg:kaId` (`KA_ID_PRED`, `:2133`), `dkg:reservedUal` (`RESERVED_UAL_PRED`, `:2139`). The `reservedKaId` is allocated **at finalize** (`:2002-2003`) and reused at mint so the on-chain kaId equals the signed id.

**Leave WM → SWM.** For collaboration, `assertionPromote` (`dkg-publisher.ts:4506`) copies the sealed content into the `_shared_memory` graph, re-applying the identical reserved-subject filter (`:4605-4607`) as defense-in-depth, and emits a gossip message. The async ingestion path instead runs `publishAsync` (`dkg-agent-publish.ts:805`): it calls `partitionPublishAsyncQuads` (`packages/agent/src/dkg-agent-helpers.ts:77`), hands public quads to `writeToWorkspace` (`dkg-agent-publish.ts:891-901`), and stores private quads in a `PrivateContentStore` (`:903-904`). `writeToWorkspace` (`dkg-publisher.ts:826`, deprecated, delegating to `share` → `_shareImpl`) serializes the public quads to N-Quads (`:907`/`:922`), wraps them in a `WorkspacePublishRequest`, inserts them into the local SWM graph (`store.insert`, `:974`), then `encodeWorkspaceGossipPayload` (`:1020`) decides encryption. For a **private** CG, `encryptWorkspacePayloadWithSenderKey` (`packages/agent/src/dkg-agent-crypto.ts:1120`, routed at `dkg-publisher.ts:1048-1058`) AEAD-encrypts the payload under a ratcheting epoch chain key minted/rotated by `createAndDistributeSwmSenderKeyEpoch` (`:1214`); the chain key is ECIES-style sealed per recipient (X25519 ephemeral-static ECDH + AEAD, `swm-sender-key.ts:163-195`) via `createSignedSwmSenderKeyPackage` (`:1788`) and sent over `PROTOCOL_SWM_SENDER_KEY`. For a **public** CG the N-Quads stay plaintext (`dkg-publisher.ts:1039`). `encodeWorkspaceGossipMessage` (`dkg-agent-crypto.ts:2355`) EOA-signs (`ethers.Wallet(...).signMessage`, `:2375`) a `GossipEnvelope` (`packages/core/src/proto/gossip-envelope.ts:31`) carrying the cleartext `contextGraphId`, sender `agentAddress`, timestamp, and the payload (`:2379-2383`). `chooseFanOutTier` (`packages/agent/src/swm/substrate-fanout.ts:197`) decides substrate (`PROTOCOL_SWM_UPDATE`, point-to-point reliable) + gossip (GossipSub mesh on the wire-hashed workspace topic). Members run `SharedMemoryHandler.handle`, verify, decrypt, and apply the N-Quads into their local SWM. A hosting core in host mode (`packages/agent/src/dkg-agent-swm-host.ts:610`) appends the **raw opaque envelope bytes** to the `SwmHostModeStore` (`packages/agent/src/swm/host-mode-store.ts:329`) — it holds no chain key and cannot decrypt.

**VM publish.** `_publish` (`dkg-agent-publish.ts:1173`) mints a `precomputedAttestation` over the kcMerkleRoot and resolves the encrypt-inline emitters (LU-5 single-blob `_resolveEncryptInlinePayload` at `:2650`, LU-11 chunked `_resolveEncryptInlineChunked` at `:2703`) for curated CGs only — in this codebase **curated == private** (`accessPolicy=1`), since `_resolveCuratedChainKeyContext` maps a PRIVATE on-chain CG to curated → encrypted inline (`:2515-2517`), so "curated CGs only" and "private CG" denote the same population. It then calls `this.publisher.publish(...)` (`dkg-publisher.ts:1704`, invoked from `_publish` at `:1268`). The publisher recomputes `kcMerkleRoot` via the shared `canonicalPublishPayload` (`packages/publisher/src/canonical-publish-payload.ts:24`, root at `:51` = `computeFlatKCRootV10(skolemizedPublicQuads, privateRoots)`, with `privateRoots` from `computePrivateRootV10`) using the same V10 keccak sort+dedupe tree (`V10MerkleTree`, `packages/core/src/crypto/v10-merkle.ts:28`) the finalize seal used — drift between them is exactly what the `expectedMerkleRoot` guard catches (`dkg-publisher.ts:2490-2493`). For curated CGs it builds the AEAD ciphertext and the separate `ciphertextChunksRoot` (`buildCiphertextChunksRoot`, `packages/core/src/crypto/v10-ciphertext-merkle.ts:197`); public quads land plaintext in `publicSnapshotStore` (`dkg-publisher.ts:989`, `:4816`). It collects a hard quorum of V10 ACKs from cores (`createV10ACKProvider`, `dkg-agent.ts:1284` → `ACKCollector.collect`, `packages/publisher/src/ack-collector.ts:137`), then submits `chain.createKnowledgeAssets` (`dkg-publisher.ts:2709`), landing in `KnowledgeAssetsLifecycle.publish` (`packages/evm-module/contracts/KnowledgeAssetsLifecycle.sol:489`). The on-chain ACK digest binds `(chainid, address(this), contextGraphId, merkleRoot, kaAmount, byteSize, epochs, tokenAmount, merkleLeafCount, ciphertextChunksRoot, ciphertextChunkCount, isImmutable)` (`sol:651-666`); a curated CG missing its ciphertext commitment reverts `CuratedCGRequiresCiphertextCommitment` (`sol:704-706`). Status flips to `confirmed` only after a successful tx (`dkg-publisher.ts:2837`); the triple is now Verified Memory.

## 4. The combined public facet

**Today (separate KA).** After a private publish confirms, `emitPublicProjectionAfterPublish` (`dkg-agent-publish.ts:1312`, invoked at `:1295`) takes `result.merkleRoot`, builds the floor with `buildPublicProjection` (`packages/agent/src/context-graph-public-projection.ts:80`), and `emitPublicProjection` (`:184`) publishes it as an **independent public KA into a different CG** (`config.publicProjectionContextGraphId`, `dkg-agent-publish.ts:1319`). Because that projection has its **own** root, it must carry `dkg:committedRoot` as an explicit triple pointing back at the private CG's root — and today this is a **mandatory** floor element (`context-graph-public-projection.ts:99`; `buildPublicProjection` throws if `committedRoot` is absent or malformed, `:88-90`). That self-reference is the only thing tying the two together.

**The combined model.** Instead of a second publish, the facet floor is unioned into the publish's `publicQuads` **before** the split, at the async attach point in `publishAsync` (`dkg-agent-publish.ts:889`, just before `partitionPublishAsyncQuads`) and the corresponding sync `publish()` entry. The floor is essentially the set `buildPublicProjection` produces: `(ual a dkg:PrivateContextGraph)`, `(ual dct:identifier UAL)`, `(ual dct:accessRights dkg:Private)` (`context-graph-public-projection.ts:96-98`), plus optional disclosure-priced tiers (`dct:publisher` `:104`, `dkg:accessService` `:107`, `dct:conformsTo` `:112`, `dkg:blindedAnchor` `:115`); IRIs in `packages/core/src/genesis.ts:243-251`. **One change to the builder is required:** as it exists today `buildPublicProjection` also emits `dkg:committedRoot` as a mandatory 4th floor triple (`:99`) and throws without it (`:88-90`); under the combined model that triple is redundant (see below), so the builder must be modified to drop it and relax the `committedRoot` validation — the combined floor is therefore *not* literally the current builder's output as-is.

**Same root, intrinsic verifiability.** Once the facet rides in `publicQuads`, `canonicalPublishPayload` commits it inside `kcMerkleRoot = computeFlatKCRootV10(skolemizedPublicQuads, privateRoots)` (`canonical-publish-payload.ts:51`) — the **same** merkle root that folds in the per-entity private hashes (`computePrivateRootV10`). The facet sits alongside the existing plaintext `PRIVATE_DATA_ANCHOR` markers (`packages/agent/src/dkg-agent-constants.ts:13`, inserted at `dkg-agent-helpers.ts:86-95`) that `partitionPublishAsyncQuads` already places for every gated root. Because the facet is now in the CG's **own** committed root, the on-chain publish record *is* the facet's verifiability edge: there is no second KA, no separate root, and therefore **no `dkg:committedRoot` self-reference and no registry/discovery CG** are required. The explicit `committedRoot` triple existed only to bridge two roots; with one root it is redundant.

**Only private quads are encrypted.** The split is unchanged: `partitionPublishAsyncQuads` separates `publicQuads` from `privateQuads` and encrypts nothing. Only `privateQuads` are folded into the root as opaque `computePrivateRootV10` hashes and shipped to cores as AEAD ciphertext (`encryptInlinePayload` for the LU-5 single-blob path, `encryptInlineChunked` for the LU-11 chunked path; emitters span `dkg-publisher.ts:1989-2017`, with the chunked emitter invoked at `:1995` and the single-blob at `:2011`; on the chunked path the chunks travel via SWM gossip rather than the ACK wire, `:2000-2002`); for private CGs the SWM gossip envelope is additionally sender-key encrypted. The public facet stays **plaintext** in `publicSnapshotStore`, committed in the clear inside the same root.

## 5. Sequence diagram — WM → SWM → VM

> Note: arrows tagged **(combined model — net-new)** describe the proposed end-state, not code that exists today. Everything else (WM write/finalize, SWM share, the private-payload AEAD ciphertext to cores, the on-chain commit) is present-day behavior for a private CG.

```mermaid
sequenceDiagram
    autonumber
    actor Agent
    participant WM as WM (local store)
    participant Members as SWM mesh (members)
    participant Cores as Hosting cores
    participant Chain as Chain (KAv10)
    actor Outsider

    Note over Agent,WM: WM write — plaintext, local only
    Agent->>WM: assertion.write quads (dkg-agent.ts:1702)<br/>PLAINTEXT, append-only (dkg-publisher.ts:4364)
    Agent->>WM: assertionFinalize seal (dkg-agent-publish.ts:1616)<br/>computeFlatKCRootV10 -> merkleRoot, EIP-712 attestation

    Note over Agent,Cores: SWM share — sender-key encrypted for private CG
    Agent->>Members: GossipEnvelope (dkg-agent-crypto.ts:2355)<br/>payload CIPHERTEXT (sender-key) - cleartext cgId + publisher
    Members-->>Agent: ACK (empty=applied / 0x01 rejected / 0x02 retryable)
    Agent->>Cores: same signed envelope (host mode)<br/>CIPHERTEXT bytes stored opaquely (host-mode-store.ts:329)
    Note over Cores: Cores hold ciphertext - cannot decrypt<br/>see only cgId, publisher, timestamp

    Note over Agent,Chain: VM publish — one merkleRoot over public facet + private payload
    Agent->>Cores: private payload AEAD CIPHERTEXT (dkg-publisher.ts:1995,2011)<br/>markers PLAINTEXT; facet PLAINTEXT publicSnapshotStore [combined model]
    Cores-->>Agent: V10 ACK signatures (ack-collector.ts:137)<br/>sign merkleRoot + ciphertextChunksRoot
    Agent->>Chain: createKnowledgeAssets (KnowledgeAssetsLifecycle.sol:489)<br/>commit merkleRoot (facet committed in SAME root [combined model])
    Chain-->>Agent: kaId, txHash (status confirmed)

    Note over Outsider,Chain: Outsider discovers + verifies the public facet [combined model — net-new]
    Outsider->>Cores: facet-only sync request (PLAINTEXT, no allowlist) [net-new]
    Cores-->>Outsider: public facet quads PLAINTEXT [net-new]<br/>(a dkg:PrivateContextGraph, UAL, accessRights Private)
    Outsider->>Chain: read committed merkleRoot
    Note over Outsider: verify facet quads in the CG's own on-chain root<br/>verifiability INTRINSIC - no committedRoot triple needed
```

## 6. Public vs private at each stage

| Stage | Plaintext / public | Encrypted / private | Who can read |
|---|---|---|---|
| **WM** | Everything (data quads + `_meta` bookkeeping) is plaintext in the local store; reserved-subject rows are WM/`_meta`-only. | Nothing is encrypted at WM; `privateQuads` from JSON-LD sit plaintext alongside `publicQuads`. | Only the owning node/agent (unshared). |
| **SWM** | Outer `GossipEnvelope` fields: version, type, cleartext `contextGraphId`, sender `agentAddress`, timestamp, signature; sender-key message metadata (epochId, membershipHash, messageIndex, nonce, aadHash). For a **public CG**, the N-Quads payload too. | For a **private CG**, the `WorkspacePublishRequest` payload (AEAD under ratcheting chain key); the chain key (ECIES-style sealed per recipient). | Members decrypt & apply; cores hold ciphertext + see envelope metadata (label, publisher, timing) only. |
| **VM** | Public quads incl. `PRIVATE_DATA_ANCHOR` markers and (combined model) the public facet floor → `publicSnapshotStore`, committed plaintext in `kcMerkleRoot`. The on-chain `merkleRoot` itself. | `privateQuads` as opaque `computePrivateRootV10` hashes in the root + AEAD ciphertext to cores; separate `ciphertextChunksRoot` for curated CGs. | Anyone reads the on-chain root + public facet; private payload gated to members who can decrypt. |

## 7. The one required capability — cores serving the public facet openly

The combined model needs exactly **one** new capability: cores (and peers) must **serve the public-facet subgraph of a private CG openly to outsiders**, even though the rest of the CG is access-gated.

Today the serve path is all-or-nothing. On the responder side, `authorizeSyncRequest` (`packages/agent/src/dkg-agent-cg-resolve.ts:779`) returns `true` only when `!isPrivateContextGraph` (`:781-782`); for a private CG it delegates to `authorizePrivateSyncRequest` (`packages/agent/src/sync/auth/request-authorize.ts:46`, called at `:785`), which validates envelope/replay/signature and then returns a single CG-wide allow/deny boolean (`:195`) — verified across the whole function body (`:46-196`), there is no subgraph carve-out. On the requester side, `buildSyncRequest` (`cg-resolve.ts:701`) sets `needsAuth = isPrivate || !hasLocalData` (`:718`), so an outsider cannot even frame an honored request for the plaintext facet. The classifier feeding both gates is `isPrivateContextGraph` (`:1211`).

The change: `authorizeSyncRequest` must grow a **facet-scoped open-serve path** — serve the bounded public-projection subgraph (the facet floor + `PRIVATE_DATA_ANCHOR` markers) without allowlist auth, while still routing everything else through `authorizePrivateSyncRequest`. Correspondingly, `buildSyncRequest` must allow an unauthenticated **facet-only** request shape. Note the existing `SyncPhase` values are `meta | snapshot | data` (`request-build.ts:3`, no `facet` phase), so a new scoped phase or scope is needed.

**Convergence with the ciphertext strip.** This capability is the same boundary the eventual "ciphertext strip" lands on: once cores stop holding private payloads at all, the public facet is the **only** part of a private CG that cores touch — so the facet-scoped open-serve path *is* the post-strip serve surface. Building the facet exception now means the strip later removes the ciphertext-holding code without re-architecting the serve gate.

## 8. Status & open questions

**Exists in code today.**
- The public/private split with plaintext markers: `partitionPublishAsyncQuads` (`dkg-agent-helpers.ts:77`) and `PRIVATE_DATA_ANCHOR` (`dkg-agent-constants.ts:13`, markers inserted at `dkg-agent-helpers.ts:86-95`).
- The single-root commit binding public quads + private hashes: `canonicalPublishPayload` (`canonical-publish-payload.ts:51`).
- "Only private quads encrypted": `encryptInlinePayload`/`encryptInlineChunked` (emitters span `dkg-publisher.ts:1989-2017`; chunked at `:1995`, single-blob at `:2011`); public quads to `publicSnapshotStore`.
- The floor builder: `buildPublicProjection` (`context-graph-public-projection.ts:80`) — note it currently mandates a `dkg:committedRoot` floor triple (`:99`) and throws without it (`:88-90`).
- The **separate-KA** projection orchestration: `emitPublicProjection` / `emitPublicProjectionAfterPublish` (`context-graph-public-projection.ts:184`, `dkg-agent-publish.ts:1312`) — publishes the floor into a *different* CG with an explicit `dkg:committedRoot`.

**What the combined model adds.**
- Union the facet floor into `publicQuads` **before** `partitionPublishAsyncQuads` / `canonicalPublishPayload` (attach point `dkg-agent-publish.ts:889`), so the facet is committed in the CG's own `kcMerkleRoot`. No code path does this today.
- Modify `buildPublicProjection` to drop the now-redundant `dkg:committedRoot` floor triple and relax its `committedRoot` validation (`:88-90`, `:99`); the combined floor cannot reuse the current builder output verbatim.
- Drop the separate projection KA, the discovery/registry CG (`config.publicProjectionContextGraphId`), and the explicit `dkg:committedRoot` triple — verifiability becomes intrinsic to the CG's own on-chain root.
- The facet-scoped open-serve path in `authorizeSyncRequest` / `buildSyncRequest` (Section 7) — net-new; confirmed absent across the full `authorizePrivateSyncRequest` body (`request-authorize.ts:46-196`), not merely unread.

**Needs devnet verification.**
- The serve-path piece: that a private CG's facet subgraph can be requested unauthenticated and served openly while the rest stays gated — this requires a new sync phase/scope and end-to-end testing against a live allowlist gate. The exact facet-bounding query (which subjects/predicates constitute "the facet" the gate may release) must be pinned so the open path cannot be used to exfiltrate gated quads.
- That an outsider can fetch the facet, read the on-chain `merkleRoot`, and verify the facet quads against it without any `committedRoot` indirection.

**Moot.** The earlier *await-vs-background* question — whether to await the separate projection publish or fire it in the background after the private publish — is now moot: the facet rides the **same** publish and the **same** merkle root, so there is no second publish to sequence and no window where the private CG is committed but its facet is not yet discoverable.
