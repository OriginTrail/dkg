apiVersion: 1
datasources:
  - name: Loki
    type: loki
    access: proxy
    url: http://loki:3100
    isDefault: true
    jsonData:
      # Surface OTLP structured metadata (dkg.*) as derived fields in Explore.
      derivedFields: []
